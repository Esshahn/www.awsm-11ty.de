{
  "name": "awsm-website",
  "version": "1.0.0",
  "description": "new version of the awsm homepage created with 11ty",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "rm -rf dist && eleventy"
  },
  "author": "Ingo Hinterding",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1",
    "@11ty/eleventy-plugin-rss": "^1.1.0"
  }
}
